<% content_for :header do %>
  <%= render HeaderComponent.new(current_user: @current_user,
                                 title: t('submitter.title'),
                                 subtitle: t('submitter.subtitle')) %>
<% end %>

<div class="row gy-2">
  <div class="col-lg-8 order-2 order-lg-1">
    <%= render Show::WelcomeBannerComponent.new(submission: @submission) %>

    <%= render Show::Step1Component.new(submission: @submission) %>
    <%= render Show::Step2Component.new(submission: @submission) %>
    <%= render Show::Step3Component.new %>
    <%= render Show::Step4Component.new(submission: @submission) %>
    <%= render Show::Step5Component.new(submission: @submission) %>
    <%= render Show::Step7Component.new(submission: @submission) %>
    <%= render Show::Step8Component.new(submission: @submission) %>
  </div>
  <div class="col-lg-4 order-1 order-lg-2">
    <%= render Shared::ProgressCardComponent.new(submission_presenter: @submission_presenter) %>
  </div>
</div>
