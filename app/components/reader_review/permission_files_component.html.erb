<%= render Shared::StepComponent.new(title: 'Permission files') do |component| %>
  <% component.with_body_content do %>
    <% if permission_files.any? %>
      <p>To open a file below, click on the filename.</p>

      <%= render Shared::PermissionFilesStepBodyTableComponent.new(submission:) %>
    <% else %>
      <p>No files provided.</p>
    <% end %>
  <% end %>
<% end %>
