<%= render Edit::StepComponent.new(edit_label: 'Undo your confirmation', data:, done_data:, edit_data:, show:, **args) do |component| %>
  <% component.with_help_content do %>
    <%= help_content %>
  <% end %>

  <% component.with_body_content do %>
    <%= form.hidden_field review_field, data: { confirm_target: 'input' } %>
    <%= body_content %>
  <% end %>
<% end %>
