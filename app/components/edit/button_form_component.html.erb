<%= helpers.with_submission_form(submission_presenter) do |form| %>
  <%= form.hidden_field field, value: %>
  <%= render Elements::ButtonComponent.new(classes:, type: 'submit', **options) do %>
    <%= label || content %>
  <% end %>
<% end %>
